[project]
name = "jasperan"
version = "0.1.0"
requires-python = ">=3.12"
dependencies = [
    "requests",
    "aiohttp",
    "PyYAML",
    "tqdm"
]

[dependency-groups]
dev = [
    "types-PyYAML"
]

[tool.uv]
